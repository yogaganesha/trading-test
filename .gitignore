# Local environment / secrets — never commit
.env
.env.*
!.env.template

# Plain-text credential notes — never commit
Alpaca API - brokerage
*credentials*
*api-keys*
*.secrets

# OS / editor cruft
.DS_Store
*.swp
*~
.idea/
.vscode/

# NOTE: NOTIFICATIONS.md is intentionally NOT ignored — per current setup,
# notifications are stored in the repo itself instead of posted to ClickUp.
